namespace Reference.Api.Shared.Features.HelloWorld.Constants;

public static class Endpoints
{
    public const string HelloWorld = nameof(HelloWorld);

    public const string HelloWorldTo = nameof(HelloWorldTo);
}
